
path: 40
row: 28
root: /data01/images/ssebop_analysis
input_root:
output_root:

api_key: 'mapzen-JmKu1BF'

satellite: LT5
year:
# Date format; string 'YYYYMMDD'
single_date: '20060706'
start_date:
end_date:
k_factor: 1.25
verify_paths: True

# Add the path relative to /input_root/
# They will be joined to input_root.
# Parameters mask and polygon are optional
polygons:
mask:
